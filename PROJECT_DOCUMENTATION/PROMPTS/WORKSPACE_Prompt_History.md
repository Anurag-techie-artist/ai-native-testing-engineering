# Workspace Prompt History

## Prompt 001
**Date/Time:** 2026-08-12 23:21:37 IST
**Purpose:** Workspace and prompt-history initialization

### Exact Prompt
# Prompt 001 — Establish Submission Workspace Structure

Before implementing, coding, researching, or solving any assignment question, establish the project workspace structure exactly as specified below.

This workspace is for the **Frugal Testing AI-Native Software Engineer Intern assignment** provided in the assignment PDF.

## 1. Root Folder

Create/use this exact root project folder:

`Manti Yoshi Anurag_SRM University AP_AP2311011041`

Do not rename it, abbreviate it, or create an alternative root folder.

## 2. Required Question Folders

Inside the root folder, create exactly these three practical-task folders:

```text
Manti Yoshi Anurag_SRM University AP_AP2311011041/
│
├── Q1_Dynamic_Canvas_WebSocket/
├── Q2_Cryptographic_Replay_API/
└── Q3_ShadowDOM_Accessibility/
```

These correspond to Q1, Q2 and Q3 of Section A of the assignment.

## 3. Internal Structure of Each Question Folder

Create the following structure inside each question folder:

```text
Q1_Dynamic_Canvas_WebSocket/
├── source/
├── prompts/
└── evidence/

Q2_Cryptographic_Replay_API/
├── source/
├── prompts/
└── evidence/

Q3_ShadowDOM_Accessibility/
├── source/
├── prompts/
└── evidence/
```

### source/

This directory will eventually contain the complete raw source code and engineering artifacts belonging only to that specific question.

Do not place code from another question here.

### prompts/

This directory will contain the exact GenAI/Antigravity prompt history for that question in Markdown format.

### evidence/

This directory will eventually contain supporting execution/evidence artifacts required for that question, if needed.

Do not generate artificial evidence at this stage.

## 4. Mandatory Prompt-History System

Prompt history is an important submission-evidence requirement.

From this interaction onward, maintain an exact chronological record of every prompt used with Antigravity for each question.

Create:

```text
Q1_Dynamic_Canvas_WebSocket/prompts/Q1_Prompt_History.md
Q2_Cryptographic_Replay_API/prompts/Q2_Prompt_History.md
Q3_ShadowDOM_Accessibility/prompts/Q3_Prompt_History.md
```

For every future prompt used to work on a question, append the exact prompt text to the corresponding Markdown history file.

Use this structure:

```markdown
# Q1 Prompt History

## Prompt 001
**Date/Time:** [timestamp]
**Purpose:** Workspace and prompt-history initialization

### Exact Prompt
[Complete exact prompt text]

---

## Prompt 002
**Date/Time:** [timestamp]
**Purpose:** [brief purpose]

### Exact Prompt
[Complete exact prompt text]

---
```

Follow the same structure for Q2 and Q3.

### Important prompt-history rules

1. Never paraphrase the prompt.
2. Never summarize the prompt instead of recording it.
3. Preserve the exact wording.
4. Maintain chronological order.
5. Never delete previous prompts.
6. Never overwrite previous prompt entries.
7. Append new prompts to the appropriate history file.
8. Keep Q1, Q2 and Q3 prompt histories completely separate.
9. Record prompts used for implementation, debugging, refinement, verification, testing, and final validation.
10. If a prompt results in no code change, still record it.
11. Do not fabricate prompts that were not actually used.
12. Do not record unrelated conversations in a question's prompt history.

The current prompt is **Prompt 001** and must be recorded as the first entry in the appropriate prompt-history file.

## 5. Assignment Boundaries

At this stage, do NOT:

* implement Q1
* implement Q2
* implement Q3
* answer Q4–Q20
* write Q21
* prepare Q22
* prepare Q23
* create application logic
* choose unnecessary frameworks
* install unnecessary dependencies
* create unnecessary source files
* create videos
* create fake test results
* create fake evidence

This interaction is strictly for **workspace organization and prompt-history initialization**.

## 6. Required Action Now

Only perform the following actions:

1. Inspect the current workspace.
2. Create the root folder if it does not already exist.
3. Create the three Q1/Q2/Q3 folders.
4. Create `source`, `prompts`, and `evidence` inside each.
5. Create the three Markdown prompt-history files.
6. Record this exact Prompt 001 in the appropriate history file.
7. Show the resulting directory tree.
8. Confirm that no implementation has been started.

Do not proceed beyond workspace initialization.

## 7. Future Working Rule

Before implementing any question, we will separately define and approve its engineering requirements.

Do not assume requirements that have not yet been provided.

Do not expand the assignment scope.

The guiding rule for this entire workspace is:

**Implement exactly what the assignment asks for — nothing more, nothing less.**

---

## Prompt 002
**Date/Time:** 2026-08-12 23:30:26 IST
**Purpose:** Additional workspace rules & engineering workflow setup

### Exact Prompt
# Additional Workspace Rules & Engineering Workflow

This is an **additional refinement to the existing workspace setup**. Do not recreate the project from scratch, delete the existing structure, or restart the workspace.

The previously established root folder and Q1/Q2/Q3 separation remain valid.

Your task in this interaction is to **update/extend the workspace organization and establish the engineering workflow rules described below**.

Do NOT begin implementing Q1, Q2, Q3, or any other assignment question yet.

---

# 1. Existing Root Workspace

The root workspace is:

`Manti Yoshi Anurag_SRM University AP_AP2311011041`

The three practical assignment tasks remain separated as:

```text
Manti Yoshi Anurag_SRM University AP_AP2311011041/
│
├── Q1_Dynamic_Canvas_WebSocket/
├── Q2_Cryptographic_Replay_API/
└── Q3_ShadowDOM_Accessibility/
```

Do not rename these folders.

Each question remains independently scoped.

---

# 2. Existing Question-Level Structure

Each Q1/Q2/Q3 folder should retain:

```text
Qx/
├── source/
├── prompts/
└── evidence/
```

The `source/` directory is exclusively for that question's implementation/artifacts.

The `prompts/` directory contains the exact chronological GenAI/Antigravity prompt history for that question.

The `evidence/` directory is reserved for legitimate supporting execution/evidence artifacts.

Do not populate `evidence/` with fabricated, unnecessary, or placeholder evidence.

---

# 3. Prompt History Is Mandatory

The assignment explicitly requires the Q1/Q2/Q3 workflow videos to show GenAI usage and prompt history, including the exact prompts, iterative queries, and AI-assisted debugging/refinement process.

Therefore, prompt history must be preserved from the beginning.

Maintain:

```text
Q1_Dynamic_Canvas_WebSocket/prompts/Q1_Prompt_History.md
Q2_Cryptographic_Replay_API/prompts/Q2_Prompt_History.md
Q3_ShadowDOM_Accessibility/prompts/Q3_Prompt_History.md
```

For every prompt used for a specific question, record the exact prompt text in that question's Markdown history.

Use chronological numbering:

```markdown
# Q1 Prompt History

## Prompt 001
**Date/Time:** ...
**Purpose:** ...

### Exact Prompt

[exact prompt]

---

## Prompt 002
**Date/Time:** ...
**Purpose:** ...

### Exact Prompt

[exact prompt]

---
```

Rules:

* Preserve exact wording.
* Never paraphrase historical prompts.
* Never silently overwrite previous entries.
* Never delete previous prompts.
* Maintain chronological order.
* Record implementation prompts.
* Record debugging prompts.
* Record refinement prompts.
* Record verification prompts.
* Record prompts that produce no code changes if they were genuinely used for the task.
* Keep Q1/Q2/Q3 histories separate.
* Never fabricate historical prompts.

The purpose is to preserve a truthful engineering trail.

---

# 4. Add a Dedicated Project Documentation Area

Create/maintain a separate documentation area at:

```text
PROJECT_DOCUMENTATION/
```

This is an internal engineering/documentation workspace and is separate from the three question implementation folders.

Create:

```text
PROJECT_DOCUMENTATION/
│
├── PRD/
│   └── CHANGE_HISTORY.md
│
├── DESIGN_BRIEF/
│   └── CHANGE_HISTORY.md
│
├── IMPLEMENTATION_DEBUG/
│   └── DEBUG_CHANGE_HISTORY.md
│
├── REFACTOR/
│   └── REFACTOR_CHANGE_HISTORY.md
│
├── GIT/
│   └── COMMIT_HISTORY.md
│
└── WORKFLOW_RULES/
    └── AI_ENGINEERING_WORKFLOW.md
```

Do not create fake versioned PRDs or design briefs yet.

The actual PRD and Design Brief will be created when we begin formally defining the relevant implementation.

---

# 5. Full PRD Versioning

The PRD will be maintained as a versioned engineering document.

When created, use a structure such as:

```text
PROJECT_DOCUMENTATION/PRD/
├── PRD_v1.0.md
├── PRD_v1.1.md
├── PRD_v1.2.md
└── CHANGE_HISTORY.md
```

Important requirements:

* Do not overwrite important historical versions.
* Increment the version when requirements materially change.
* Record what changed and why in `CHANGE_HISTORY.md`.
* The PRD must remain the authoritative source for what is required.
* Requirements must not be invented merely to make the project appear more sophisticated.

---

# 6. Full Design Brief Versioning

The Design Brief is separate from the PRD.

The distinction is:

**PRD = WHAT must be built.**

**Design Brief = HOW the solution is intended to be structured/designed.**

When created, maintain versions such as:

```text
PROJECT_DOCUMENTATION/DESIGN_BRIEF/
├── DESIGN_BRIEF_v1.0.md
├── DESIGN_BRIEF_v1.1.md
└── CHANGE_HISTORY.md
```

Material design changes must be versioned and recorded.

Do not create the Design Brief until the relevant requirements have been properly defined.

---

# 7. Expected vs Actual Implementation Debugging

During implementation, maintain:

```text
PROJECT_DOCUMENTATION/IMPLEMENTATION_DEBUG/
└── EXPECTED_VS_ACTUAL.md
```

This document must compare implementation behavior against the expected behavior.

For meaningful implementation/debugging issues, use a structure similar to:

```markdown
## Issue #001

### Expected
...

### Actual
...

### Difference
...

### Root Cause
...

### Fix Applied
...

### Verification
...

### Status
RESOLVED
```

The purpose is to create a factual engineering record rather than simply saying that something was "fixed."

Do not create artificial issues.

Only record real implementation/debugging discrepancies.

---

# 8. Dead Code Cleanup & Refactoring

Do not continuously refactor code while functionality is still unstable.

Use this workflow:

```text
IMPLEMENT
↓
TEST
↓
VERIFY
↓
IDENTIFY DEAD CODE / STRUCTURAL ISSUES
↓
REFACTOR
↓
RUN TESTS AGAIN
↓
VERIFY NO REGRESSION
```

Maintain:

```text
PROJECT_DOCUMENTATION/REFACTOR/
└── DEAD_CODE_CLEANUP.md
```

Record meaningful cleanup/refactoring actions and their verification.

Do not refactor merely for cosmetic reasons.

Do not remove code simply because it appears unused without checking whether it is required by the implementation or test workflow.

---

# 9. Git Commit Discipline

Maintain:

```text
PROJECT_DOCUMENTATION/GIT/
└── COMMIT_HISTORY.md
```

Git commits should represent meaningful engineering changes.

Prefer clean conventional-style commit messages such as:

```text
feat(q1): add canvas websocket testbed
feat(q1): implement websocket jitter injection
test(q1): validate canvas state transition
fix(q1): handle coordinate drift during repaint
refactor(q1): remove unused interaction helpers
docs: update q1 implementation documentation
```

Do not create meaningless commits such as:

```text
final
final2
working
test
changes
fixed
update
```

Do not create Git commits merely to increase the commit count.

---

# 10. Engineering Workflow Rule

Create:

```text
PROJECT_DOCUMENTATION/WORKFLOW_RULES/AI_ENGINEERING_WORKFLOW.md
```

This should contain the reusable engineering rules for this workspace.

The core workflow is:

```text
UNDERSTAND
↓
DEFINE REQUIREMENTS
↓
PLAN
↓
IMPLEMENT
↓
TEST
↓
COMPARE EXPECTED vs ACTUAL
↓
DEBUG
↓
VERIFY
↓
REFACTOR / CLEAN UP
↓
REGRESSION TEST
↓
DOCUMENT
↓
CLEAN GIT COMMIT
```

Do not skip verification simply because the implementation appears to work.

---

# 11. Scope-Lock Rule

This is one of the most important rules for the entire assignment.

The guiding principle is:

**Implement exactly what the assignment asks for — nothing more, nothing less.**

Therefore:

* Do not add unnecessary features.
* Do not add unnecessary UI.
* Do not add unnecessary dependencies.
* Do not introduce unnecessary architecture.
* Do not add unrelated APIs.
* Do not expand the scope because a feature seems interesting.
* Do not solve future questions while working on the current question.
* Do not implement Q2 while working on Q1.
* Do not implement Q3 while working on Q1/Q2.
* Do not optimize for "impressiveness" instead of assignment compliance.

If a requirement is ambiguous, identify the ambiguity and ask for clarification rather than inventing additional scope.

---

# 12. Evidence Rule

Never claim that an acceptance criterion has passed unless there is actual evidence.

Evidence may include:

* test output
* terminal output
* browser behavior
* assertions
* logs
* screenshots where appropriate
* reproducible execution

Do not manufacture success output.

Do not weaken an assertion merely to obtain a passing test.

---

# 13. Documentation Rule

Documentation should reflect the actual engineering state.

Do not write documentation first and then force the implementation to match fictional behavior.

For major implementation changes:

```text
Implementation
↓
Verification
↓
Documentation update
```

For requirement/design changes:

```text
Requirement/design change
↓
Version document
↓
Record change history
↓
Implementation
```

---

# 14. Separation of Assignment Deliverables and Internal Documentation

The following are internal engineering records unless the assignment explicitly requires them:

* PRD versions
* Design Brief versions
* debugging records
* refactoring records
* Git documentation
* workflow rules
* internal evidence artifacts

Do not automatically place all of these into the final submission.

The assignment's explicit Section A submission requirement remains:

* Q1: source code + one sequential workflow video
* Q2: source code + one sequential workflow video
* Q3: engineering artifacts/scripts + one sequential workflow video

Each receives its own Google Drive folder link in the final unified PDF.

---

# 15. Final Submission Separation

Eventually, the root workspace may contain:

```text
Manti Yoshi Anurag_SRM University AP_AP2311011041/
│
├── Q1_Dynamic_Canvas_WebSocket/
├── Q2_Cryptographic_Replay_API/
├── Q3_ShadowDOM_Accessibility/
│
├── PROJECT_DOCUMENTATION/
│
└── FINAL_SUBMISSION/
```

The final submission area is reserved for the actual final submission material.

Do not generate the final PDF yet.

---

# 16. Reusable Rule / Skill Principle

Do not require these rules to be repeated in every future prompt.

Treat the important workflow rules in:

`PROJECT_DOCUMENTATION/WORKFLOW_RULES/AI_ENGINEERING_WORKFLOW.md`

as persistent project-level operating rules.

When working on future tasks:

1. Follow these rules automatically.
2. Do not ask the user to repeat them.
3. Do not require the user to repeatedly remind you to save prompt history.
4. Do not require the user to repeatedly remind you about scope control.
5. Do not require the user to repeatedly remind you about verification and documentation.
6. Apply the rules unless the user explicitly changes them.

However, do NOT treat every temporary implementation instruction as a permanent rule.

Only important, reusable engineering rules should become persistent workflow rules.

---

# 17. Current Action — NOTHING BEYOND SETUP

For this interaction only:

1. Inspect the existing workspace.
2. Preserve the existing structure.
3. Add the `PROJECT_DOCUMENTATION` structure described above.
4. Create/update `AI_ENGINEERING_WORKFLOW.md` with these reusable rules.
5. Preserve the existing Q1/Q2/Q3 prompt-history system.
6. Do not implement any assignment question.
7. Do not create application code.
8. Do not install implementation dependencies.
9. Do not create fake PRD/design content.
10. Do not create fake evidence.
11. Do not create videos.
12. Do not modify assignment answers.

Finally, show the resulting directory tree and briefly report what was added or changed.

Stop after workspace/process setup.

The next interaction will separately define the requirements and PRD for the first implementation task.

---

## Prompt 003
**Date/Time:** 2026-08-12 23:37:13 IST
**Purpose:** Additional workspace correction — prompt history separation

### Exact Prompt
# Additional Workspace Correction — Prompt History Separation

Make the following corrections to the existing workspace setup.

Do NOT start Q1, Q2, Q3, or any other assignment implementation.

Do NOT delete the existing engineering rules.

Do NOT recreate the workspace from scratch.

---

## 1. Keep the Existing Persistent Agent Rules

Keep:

```text
.agents/AGENTS.md
```

and:

```text
PROJECT_DOCUMENTATION/WORKFLOW_RULES/AI_ENGINEERING_WORKFLOW.md
```

The existing scope-lock, engineering workflow, verification, documentation, refactoring, Git, and evidence rules are correct.

Do not weaken or remove them.

---

# 2. Correct Prompt-History Architecture

The current Q1/Q2/Q3 prompt-history files contain the workspace initialization prompt.

That prompt is a **workspace-level prompt**, not a question-specific engineering prompt.

Separate workspace-level prompts from question-specific prompts.

Create:

```text
PROJECT_DOCUMENTATION/
└── PROMPTS/
    └── WORKSPACE_Prompt_History.md
```

Use this file for prompts concerning the overall project/workspace, including:

* workspace initialization
* global workflow rules
* project documentation rules
* Git setup
* global engineering rules
* persistent agent instructions
* global process changes

---

# 3. Question-Specific Prompt Histories

Keep:

```text
Q1_Dynamic_Canvas_WebSocket/prompts/Q1_Prompt_History.md

Q2_Cryptographic_Replay_API/prompts/Q2_Prompt_History.md

Q3_ShadowDOM_Accessibility/prompts/Q3_Prompt_History.md
```

These files must contain ONLY prompts specifically related to the respective question.

Therefore:

* Q1 history = Q1 prompts only.
* Q2 history = Q2 prompts only.
* Q3 history = Q3 prompts only.
* Workspace history = global workspace prompts.

Do not duplicate the same global prompt across all three question histories.

---

# 4. Preserve the Existing Prompt — Do Not Lose History

The existing workspace initialization prompt must NOT be discarded.

Move/copy that existing Prompt 001 into:

```text
PROJECT_DOCUMENTATION/PROMPTS/WORKSPACE_Prompt_History.md
```

with its exact original wording preserved.

Do not paraphrase it.

Do not rewrite it.

Do not silently alter its content.

The goal is to reorganize the history, not erase it.

---

# 5. Reset Question-Specific Histories Correctly

Because no actual Q1, Q2, or Q3 engineering work has started yet, the question-specific histories should not contain workspace-level implementation/setup prompts.

After the workspace prompt has been preserved globally, the Q1/Q2/Q3 prompt histories should be ready to receive their first actual question-specific prompt.

Do not invent Prompt 001 for Q1/Q2/Q3.

The first real Q1 engineering prompt will become Q1 Prompt 001.

The first real Q2 engineering prompt will become Q2 Prompt 001.

The first real Q3 engineering prompt will become Q3 Prompt 001.

---

# 6. Future Prompt-Logging Rule

From this point forward, classify every prompt before recording it:

### Workspace-level

Record in:

`PROJECT_DOCUMENTATION/PROMPTS/WORKSPACE_Prompt_History.md`

### Q1-specific

Record in:

`Q1_Dynamic_Canvas_WebSocket/prompts/Q1_Prompt_History.md`

### Q2-specific

Record in:

`Q2_Cryptographic_Replay_API/prompts/Q2_Prompt_History.md`

### Q3-specific

Record in:

`Q3_ShadowDOM_Accessibility/prompts/Q3_Prompt_History.md`

Never duplicate a prompt between histories unless the same prompt was genuinely used independently for multiple contexts.

Preserve exact wording.

---

# 7. UTF-8 Encoding

Ensure all Markdown documentation and prompt-history files are saved as valid UTF-8 text.

Characters such as:

```text
→
↓
—
│
├
└
```

must render correctly and must not appear as mojibake such as:

```text
â†“
â€”
â”‚
â”œ
â””
```

Do not change the semantic content merely for formatting.

---

# 8. Verify Before Finishing

After making these corrections:

1. Show the updated directory tree.
2. Show the line count of:

   * `WORKSPACE_Prompt_History.md`
   * `Q1_Prompt_History.md`
   * `Q2_Prompt_History.md`
   * `Q3_Prompt_History.md`
3. Confirm that the workspace initialization prompt exists only in the workspace history.
4. Confirm that Q1/Q2/Q3 histories contain no unrelated workspace-level prompt.
5. Confirm that Markdown files are UTF-8 encoded.
6. Do not modify `.agents/AGENTS.md` unless required for this correction.
7. Do not implement any assignment question.
8. Do not install implementation dependencies.
9. Do not create implementation source code.
10. Do not commit or push anything.

Stop after verification.

---
